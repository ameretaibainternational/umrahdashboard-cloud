In hotel settings, when we add a new hotel, it autofills the other prices by checking room price and then dividing it that is good. But On Hotel Vouher page, there is an option to add Hotel from there too. That popup to add hotel is not doing this automation thing. make it do this too.

In Hotel Voucher Page, When we select a Hotel Contcat from dropdown, then there is no option to unselect that. fix this too.

In Hotels Tab in Settings, there is Contact Number Field in that list. But we have hotel contacts already on saperate page so remove the Contact Number field from Hotels tab.

When we try to add a new hotel contact in settings, it asks for Hotel Name, city and contact number. Make the Hotel Name from text to dropdown and fetch hotel names from added hotels in settings.

In Visa Rate Settings, Remove the Child Visa rate Entorely because Adult and child visa rates are same. Also, Where there is 6-59 PAx, change it to 6-49 PAX and also change claculations according to these changes.

In package calculator when we add a custom ticket of like 2 lac pkr, and then we change pax to 2, then it divies 2 lac on 2 pax and makes 1 lac per pax that is wrong. make it multiply like if there are 2 pax then 2lax x2 = 4lac total amount for tickets for 2 pax.

On Mobile view, there is too much empty space below invoice preview.

Change the confirmation number heading in hotel voucher to QTY

